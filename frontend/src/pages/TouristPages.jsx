import { useMemo, useState } from 'react';
import { Activity, ArrowRight, BadgeCheck, BellRing, BrainCircuit, CalendarDays, Check, ChevronRight, CircleHelp, CloudOff, Compass, Download, HeartPulse, LocateFixed, LockKeyhole, MapPinned, Navigation, Phone, Radio, Route, ShieldCheck, Siren, Smartphone, Sparkles, Timer, TriangleAlert, UserRound, UsersRound, Wifi } from 'lucide-react';
import { Link } from 'react-router-dom';
import Shell from '../components/shared/Shell';
import MapSurface from '../components/shared/MapSurface';
import { DataRow, EmptyState, MetricCard, PageIntro, Panel, RiskProgress, StatusPill, Timeline } from '../components/shared/Primitives';
import Modal from '../components/shared/Modal';
import RiskBadge from '../components/shared/RiskBadge';
import DigitalID from '../components/tourist/DigitalID';
import SOSButton from '../components/tourist/SOSButton';
import { useApp } from '../store/AppContext';

function usePrimaryTourist() {
  const { state } = useApp();
  return state.tourists.find((tourist) => tourist.touristId === 'SY-1001') || state.tourists[0];
}

function TouristHeader({ eyebrow, title, description, action }) {
  return <PageIntro eyebrow={eyebrow} title={title} description={description} action={action} />;
}

export function TouristDashboard() {
  const { state, dispatch, simulateMovement, simulateInactivity, triggerSOS } = useApp();
  const tourist = usePrimaryTourist();
  const activeIncident = state.incidents.find((incident) => incident.touristId === tourist.touristId && incident.status !== 'resolved');
  const [showSOS, setShowSOS] = useState(false);
  const zoneTone = tourist.zoneType === 'danger' ? 'critical' : tourist.zoneType === 'caution' ? 'caution' : 'safe';

  return <Shell eyebrow="Tourist workspace" title={`Good morning, ${tourist.name.split(' ')[0]}.`}><TouristHeader eyebrow="Your active journey" title="You are covered, even between signals." description="Keep your journey moving. SafeYatra is watching location, risk and network health in the background." action={<button onClick={() => dispatch({ type: 'SET_DEMO', patch: { journeyActive: !state.demo.journeyActive } })} className="inline-flex min-h-11 items-center gap-2 bg-sy-accent px-4 text-xs font-extrabold text-sy-bg hover:bg-white"><Route size={15} />{state.demo.journeyActive ? 'Pause journey' : 'Start journey'}</button>} /><div className="grid gap-4 xl:grid-cols-[1.05fr_1.65fr_0.9fr]"><section className={`border p-5 ${zoneTone === 'critical' ? 'border-red-300/40 bg-red-500/[0.08]' : zoneTone === 'caution' ? 'border-amber-300/30 bg-amber-300/[0.06]' : 'border-sy-accent/30 bg-sy-accent/[0.06]'}`}><div className="flex items-start justify-between gap-3"><div><p className="sy-label">Current safety</p><div className="mt-4 flex items-center gap-3"><span className={`grid h-11 w-11 place-items-center rounded-full ${zoneTone === 'critical' ? 'bg-red-500/20 text-red-200' : zoneTone === 'caution' ? 'bg-amber-400/20 text-amber-100' : 'bg-sy-accent/20 text-sy-accent'}`}><ShieldCheck size={22} /></span><div><p className="text-2xl font-extrabold text-white">{tourist.riskLevel === 'SAFE' ? 'You are safe' : `${tourist.riskLevel} risk`}</p><p className="mt-1 text-xs text-white/50">{tourist.zoneName}</p></div></div></div><StatusPill tone={state.networkStatus === 'offline' ? 'offline' : 'safe'} icon={state.networkStatus === 'offline' ? CloudOff : Wifi}>{state.networkStatus === 'offline' ? 'Local mode' : 'Protected'}</StatusPill></div><div className="mt-8"><RiskProgress score={tourist.riskScore} level={tourist.riskLevel} /></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="border border-sy-border bg-sy-bg/25 p-3"><p className="sy-label">Location</p><p className="mt-2 font-mono text-xs text-white">{tourist.location.latitude.toFixed(4)}</p><p className="font-mono text-xs text-white/50">{tourist.location.longitude.toFixed(4)}</p></div><div className="border border-sy-border bg-sy-bg/25 p-3"><p className="sy-label">Last check-in</p><p className="mt-2 font-mono text-xs text-white">{tourist.lastCheckIn}</p><p className="text-xs text-white/50">GPS active</p></div></div><button onClick={() => setShowSOS(true)} className="mt-5 flex min-h-12 w-full items-center justify-center gap-2 bg-red-500 px-4 text-sm font-extrabold text-white transition hover:bg-white hover:text-red-600"><Siren size={17} /> Open emergency SOS</button></section><MapSurface title="Journey map" subtitle={`${tourist.journey} · ${tourist.journeyProgress}% complete`} center={[tourist.location.latitude, tourist.location.longitude]} tourists={[tourist]} zones={state.zones} routePoints={[[19.9282, 73.5214], [19.9322, 73.5304], [19.9386, 73.5425], [19.9494, 73.5568], [tourist.location.latitude, tourist.location.longitude]]} height="400px" overlay={<><p className="sy-label text-sy-accent">You are here</p><p className="mt-1 text-xs font-bold text-white">{tourist.destination}</p><p className="mt-1 font-mono text-[10px] text-white/45">{tourist.location.latitude.toFixed(4)} · {tourist.location.longitude.toFixed(4)}</p></>} /><div className="space-y-4"><Panel eyebrow="Journey status" title="On route"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center border border-sy-accent/30 bg-sy-accent/10 text-sy-accent"><Navigation size={18} /></div><div><p className="text-sm font-bold text-white">{tourist.destination}</p><p className="text-xs text-white/45">{tourist.journeyProgress}% complete</p></div></div><div className="mt-4 h-1.5 bg-sy-bg"><div className="h-full bg-sy-accent" style={{ width: `${tourist.journeyProgress}%` }} /></div><div className="mt-4 flex justify-between text-[10px] text-white/40"><span>Nashik</span><span>{tourist.destination}</span></div></Panel><Panel eyebrow="Safety actions" title="Try the local signals"><div className="space-y-2"><button onClick={simulateMovement} className="flex min-h-10 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><span className="flex items-center gap-2"><LocateFixed size={15} className="text-sy-accent" /> Simulate GPS movement</span><ChevronRight size={14} /></button><button onClick={simulateInactivity} className="flex min-h-10 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/70 hover:border-amber-300/40 hover:text-white"><span className="flex items-center gap-2"><Timer size={15} className="text-sy-warning" /> Simulate inactivity</span><ChevronRight size={14} /></button><Link to="/tourist/safety" className="flex min-h-10 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><span className="flex items-center gap-2"><Activity size={15} className="text-sy-blue" /> Open risk engine</span><ArrowRight size={14} /></Link></div></Panel></div></div><div className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Risk score" value={`${tourist.riskScore}%`} detail="Live local calculation" tone={tourist.riskLevel === 'SAFE' ? 'safe' : 'high'} icon={HeartPulse} /><MetricCard label="Digital ID" value="VERIFIED" detail="Hash available to responders" tone="safe" icon={BadgeCheck} /><MetricCard label="Emergency contact" value="READY" detail={tourist.emergencyContact} tone="blue" icon={Phone} /><MetricCard label="Network" value={state.networkStatus.toUpperCase()} detail={`${state.pendingSync} pending packets`} tone={state.networkStatus === 'offline' ? 'neutral' : 'accent'} icon={state.networkStatus === 'offline' ? CloudOff : Wifi} /></div>{activeIncident && <div className="mt-4 border border-red-300/30 bg-red-500/[0.06] p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p className="sy-label text-red-200">Open emergency</p><p className="mt-1 text-sm font-bold text-white">{activeIncident.incidentId} · {activeIncident.reason}</p></div><Link to="/tourist/emergency" className="inline-flex items-center gap-2 text-xs font-bold text-red-100">View response status <ArrowRight size={14} /></Link></div></div>}<Modal open={showSOS} onClose={() => setShowSOS(false)} eyebrow="Emergency workflow" title="Are you sure you want to send an emergency alert?" actions={<><button onClick={() => setShowSOS(false)} className="min-h-11 border border-sy-border px-4 text-xs font-bold text-white/60 hover:text-white">Cancel</button><button onClick={() => { triggerSOS({ touristId: tourist.touristId, source: 'SOS' }); setShowSOS(false); }} className="min-h-11 bg-red-500 px-4 text-xs font-extrabold text-white hover:bg-red-400">Confirm SOS</button></>}><div className="border border-red-300/30 bg-red-500/[0.06] p-4"><div className="flex items-start gap-3"><Siren size={20} className="mt-0.5 text-red-200" /><div><p className="text-sm font-bold text-white">Your live location will be captured.</p><p className="mt-1 text-xs leading-5 text-white/50">SafeYatra will notify the authority control room and your emergency contact. If offline, the alert is stored locally for automatic sync.</p></div></div></div><div className="mt-4 grid gap-2 text-xs"><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> GPS coordinates captured</div><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> Digital ID attached privately</div><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> Escalation chain ready</div></div></Modal></Shell>;
}

export function TouristJourney() {
  const { state, dispatch, simulateMovement } = useApp();
  const tourist = usePrimaryTourist();
  const routePoints = [[19.9282, 73.5214], [19.9322, 73.5304], [19.9386, 73.5425], [19.9494, 73.5568], [tourist.location.latitude, tourist.location.longitude]];
  return <Shell eyebrow="Tourist workspace" title="My journey" actions={<StatusPill tone={state.demo.journeyActive ? 'safe' : 'offline'} icon={Route}>{state.demo.journeyActive ? 'Journey active' : 'Journey paused'}</StatusPill>}><TouristHeader eyebrow="Location & check-ins" title="Your route, in your hands." description="A calm view of your planned route, check-in rhythm and the local signals that protect it." action={<button onClick={() => dispatch({ type: 'SET_DEMO', patch: { journeyActive: !state.demo.journeyActive } })} className="inline-flex min-h-11 items-center gap-2 border border-sy-border px-4 text-xs font-bold text-white hover:border-sy-accent/50"><Route size={15} />{state.demo.journeyActive ? 'Pause journey' : 'Resume journey'}</button>} /><div className="grid gap-4 xl:grid-cols-[1.5fr_0.8fr]"><MapSurface title="Route telemetry" subtitle={`${tourist.journey} · expected arrival 10:20 UTC`} center={[tourist.location.latitude, tourist.location.longitude]} tourists={[tourist]} zones={state.zones} routePoints={routePoints} height="520px" /><div className="space-y-4"><Panel eyebrow="Journey details" title="Nashik → Trimbakeshwar"><DataRow label="Current destination" value={tourist.destination} /><DataRow label="Progress" value={`${tourist.journeyProgress}%`} mono /><DataRow label="Next check-in" value="09:00 UTC" mono /><DataRow label="Travel mode" value="Walking + local transit" /></Panel><Panel eyebrow="Route actions" title="Keep your signal fresh"><button onClick={simulateMovement} className="flex min-h-11 w-full items-center justify-center gap-2 bg-sy-accent text-xs font-extrabold text-sy-bg hover:bg-white"><LocateFixed size={15} /> Send a location update</button><div className="mt-3 border border-sy-border bg-sy-bg-soft p-3 text-xs leading-5 text-white/50">Location updates are cached locally first. Your journey stays visible to you even if the network drops.</div></Panel><Panel eyebrow="Journey milestones" title="Check-in trail"><Timeline compact items={[{ status: 'resolved', label: 'Journey started', time: '07:42', actor: 'Aarav Mehta' }, { status: 'acknowledged', label: 'Temple corridor entered', time: '08:08', actor: 'Geo-fence' }, { status: 'detected', label: 'Next check-in window', time: '09:00', actor: 'SafeYatra AI' }]} /></Panel></div></div></Shell>;
}

export function TouristSafety() {
  const { state, simulateHighRisk, simulateInactivity, enterDangerZone } = useApp();
  const tourist = usePrimaryTourist();
  const [weather, setWeather] = useState(state.demo.weather);
  return <Shell eyebrow="Tourist workspace" title="Safety monitor"><TouristHeader eyebrow="AI risk engine" title="Understand the signal before it becomes an alert." description="This score is explainable: place, movement, time and weather contribute to one clear action level." action={<StatusPill tone={tourist.riskLevel === 'SAFE' ? 'safe' : tourist.riskLevel === 'CRITICAL' ? 'critical' : 'caution'} icon={BrainCircuit}>{tourist.riskLevel} risk</StatusPill>} /><div className="grid gap-4 xl:grid-cols-[1fr_0.9fr]"><Panel eyebrow="Live risk analysis" title="Aarav Mehta · SY-1001" tone={tourist.riskLevel === 'CRITICAL' ? 'critical' : 'default'}><div className="flex flex-col gap-8 sm:flex-row sm:items-center"><div className="grid h-40 w-40 shrink-0 place-items-center rounded-full border-[10px] border-sy-border" style={{ background: `conic-gradient(${tourist.riskLevel === 'SAFE' ? '#2fc77a' : tourist.riskLevel === 'HIGH' ? '#f28b46' : '#ef5b5b'} ${tourist.riskScore * 3.6}deg, transparent 0)` }}><div className="grid h-28 w-28 place-items-center rounded-full bg-sy-card text-center"><span className="font-mono text-3xl font-bold text-white">{tourist.riskScore}%</span><span className="sy-label mt-1">risk score</span></div></div><div className="flex-1"><RiskBadge level={tourist.riskLevel} score={tourist.riskScore} size="lg" /><p className="mt-4 max-w-md text-sm leading-6 text-white/55">The local engine is active and calculating with the latest location snapshot. A warning is shown before automatic escalation.</p><div className="mt-5 space-y-2">{(tourist.riskLevel === 'SAFE' ? ['Inside a patrolled safe corridor', 'GPS movement looks normal', 'No severe weather detected'] : ['Entered danger zone', 'No movement for 25 minutes', 'Weather risk detected']).map((reason) => <div key={reason} className="flex items-center gap-2 text-xs text-white/70"><Check size={14} className="text-sy-accent" />{reason}</div>)}</div></div></div></Panel><Panel eyebrow="Simulation lab" title="Test the risk engine"><div className="space-y-3"><label className="block"><span className="mb-2 block text-xs font-bold text-white/60">Weather context</span><select value={weather} onChange={(event) => setWeather(event.target.value)} className="h-11 w-full border border-sy-border bg-sy-panel px-3 text-sm text-white"><option value="clear">Clear</option><option value="cloudy">Cloudy</option><option value="heavy-rain">Heavy rain</option><option value="storm">Storm</option></select></label><button onClick={simulateHighRisk} className="flex min-h-11 w-full items-center justify-between border border-red-300/30 bg-red-500/[0.05] px-3 text-xs font-bold text-red-100 hover:bg-red-500/[0.1]"><span className="flex items-center gap-2"><TriangleAlert size={15} /> Simulate high risk</span><ArrowRight size={14} /></button><button onClick={enterDangerZone} className="flex min-h-11 w-full items-center justify-between border border-orange-300/30 px-3 text-xs font-bold text-orange-100 hover:bg-orange-400/[0.08]"><span className="flex items-center gap-2"><MapPinned size={15} /> Move into danger zone</span><ArrowRight size={14} /></button><button onClick={simulateInactivity} className="flex min-h-11 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><span className="flex items-center gap-2"><Timer size={15} /> Simulate 25 min inactivity</span><ArrowRight size={14} /></button></div></Panel></div><div className="mt-4 grid gap-4 md:grid-cols-3"><MetricCard label="Geo-fence" value={tourist.zoneName.split(' · ')[1] || 'SAFE'} detail="Local zone check active" icon={MapPinned} tone={tourist.zoneType === 'danger' ? 'critical' : 'safe'} /><MetricCard label="Movement" value={state.demo.inactivityMins ? `${state.demo.inactivityMins}m idle` : 'NORMAL'} detail="Last GPS refresh just now" icon={Activity} tone={state.demo.inactivityMins ? 'caution' : 'safe'} /><MetricCard label="Weather model" value={weather.toUpperCase()} detail="Local risk contribution" icon={CloudOff} tone={weather === 'clear' ? 'safe' : 'caution'} /></div></Shell>;
}

export function TouristDigitalId() {
  const tourist = usePrimaryTourist();
  return <Shell eyebrow="Tourist workspace" title="Digital tourist ID"><TouristHeader eyebrow="Identity & trust" title="Your verified identity, without exposing your profile." description="Share a scannable proof of identity with authorized responders. Sensitive data stays off-chain." action={<StatusPill tone="safe" icon={BadgeCheck}>Verified</StatusPill>} /><div className="grid gap-4 lg:grid-cols-[0.9fr_1.1fr]"><div><DigitalID tourist={{ ...tourist, digitalId: 'SY-2026-8F92A1', qrCode: '8F92A1-SAFEYATRA', destination: tourist.destination }} /></div><div className="space-y-4"><Panel eyebrow="What is shared" title="Privacy-safe verification"><div className="space-y-1"><DataRow label="Tourist ID" value="SY-2026-8F92A1" mono /><DataRow label="Status" value="VERIFIED ✓" valueClass="text-sy-success" /><DataRow label="Blockchain hash" value="0x8f92...a1b3" mono /><DataRow label="Last verification" value="26 Jul 2026 · 08:36 UTC" mono /></div></Panel><Panel eyebrow="Your controls" title="Use your ID when you need it"><div className="grid gap-2 sm:grid-cols-2"><button className="flex min-h-11 items-center justify-center gap-2 border border-sy-border text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><Download size={15} /> Download ID</button><button className="flex min-h-11 items-center justify-center gap-2 border border-sy-border text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><Smartphone size={15} /> Show QR</button></div><div className="mt-4 border border-sy-accent/25 bg-sy-accent/[0.05] p-4"><div className="flex gap-3"><LockKeyhole size={17} className="mt-0.5 text-sy-accent" /><p className="text-xs leading-5 text-white/55">Responders see a verified hash and only the details needed to help. You stay in control of the rest.</p></div></div></Panel></div></div></Shell>;
}

export function TouristEmergency() {
  const { state, triggerSOS } = useApp();
  const tourist = usePrimaryTourist();
  const [showConfirm, setShowConfirm] = useState(false);
  const [created, setCreated] = useState(state.incidents.find((incident) => incident.touristId === tourist.touristId && incident.source === 'SOS'));
  const incident = created || state.incidents.find((item) => item.touristId === tourist.touristId && item.status !== 'resolved');
  function confirm() { const newIncident = triggerSOS({ touristId: tourist.touristId, source: 'SOS' }); setCreated(newIncident); setShowConfirm(false); }
  return <Shell eyebrow="Tourist workspace" title="Emergency SOS"><TouristHeader eyebrow="One-tap escalation" title="Help is closer than your signal." description="Use SOS when you are in danger. The system captures your location, attaches your verified ID and escalates to the authority workflow." action={state.networkStatus === 'offline' ? <StatusPill tone="offline" icon={CloudOff}>Offline SOS available</StatusPill> : <StatusPill tone="safe" icon={Wifi}>Connected SOS</StatusPill>} /><div className="grid gap-4 xl:grid-cols-[0.8fr_1.2fr]"><Panel eyebrow="Emergency action" title="Send an alert now" tone="critical"><div className="flex flex-col items-center py-4 text-center"><div className="relative"><div className="absolute inset-0 rounded-full bg-red-500/20 animate-pulse-ring" /><button onClick={() => setShowConfirm(true)} className="relative grid h-44 w-44 place-items-center rounded-full border-8 border-red-300/30 bg-red-500 text-2xl font-extrabold text-white shadow-2xl transition hover:scale-[1.02] hover:bg-white hover:text-red-500">SOS</button></div><p className="mt-6 text-sm font-bold text-white">Tap to open confirmation</p><p className="mt-2 max-w-xs text-xs leading-5 text-white/45">Hold-to-trigger is available below for a faster repeat alert.</p><div className="mt-6 w-full max-w-xs"><SOSButton tourist={tourist} location={tourist.location} onTriggered={(result) => { const newIncident = triggerSOS({ touristId: tourist.touristId, source: 'SOS' }); setCreated(newIncident); }} /></div></div></Panel><Panel eyebrow="Escalation chain" title="What happens next"><div className="space-y-0"><div className="flex items-start gap-4 border-b border-sy-border py-4"><span className="grid h-8 w-8 shrink-0 place-items-center border border-sy-accent/30 bg-sy-accent/10 text-sy-accent"><LocateFixed size={15} /></span><div><p className="text-sm font-bold text-white">Location captured</p><p className="mt-1 text-xs leading-5 text-white/45">{tourist.location.latitude.toFixed(4)}, {tourist.location.longitude.toFixed(4)}</p></div></div><div className="flex items-start gap-4 border-b border-sy-border py-4"><span className="grid h-8 w-8 shrink-0 place-items-center border border-sy-blue/30 bg-sy-blue/10 text-sy-blue"><Radio size={15} /></span><div><p className="text-sm font-bold text-white">Authority notified</p><p className="mt-1 text-xs leading-5 text-white/45">The incident appears in the command center queue.</p></div></div><div className="flex items-start gap-4 py-4"><span className="grid h-8 w-8 shrink-0 place-items-center border border-sy-warning/30 bg-sy-warning/10 text-sy-warning"><UsersRound size={15} /></span><div><p className="text-sm font-bold text-white">Responder coordinated</p><p className="mt-1 text-xs leading-5 text-white/45">A field unit receives the location and risk context.</p></div></div></div></Panel></div>{incident && <div className="mt-4 border border-red-300/30 bg-red-500/[0.06] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="sy-label text-red-200">SOS alert created</p><h3 className="mt-2 font-mono text-xl font-bold text-white">{incident.incidentId}</h3><p className="mt-2 text-sm text-white/55">{incident.message}</p></div><StatusPill tone={state.networkStatus === 'offline' ? 'offline' : 'critical'}>{state.networkStatus === 'offline' ? 'Pending sync' : 'Detected'}</StatusPill></div><div className="mt-5 grid gap-3 text-xs sm:grid-cols-3"><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> Location captured</div><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> Authority notified</div><div className="flex items-center gap-2 text-white/60"><Check size={14} className="text-sy-success" /> Emergency contact notified</div></div></div>}<Modal open={showConfirm} onClose={() => setShowConfirm(false)} eyebrow="Immediate response" title="Are you sure you want to send an emergency alert?" actions={<><button onClick={() => setShowConfirm(false)} className="min-h-11 border border-sy-border px-4 text-xs font-bold text-white/60 hover:text-white">Cancel</button><button onClick={confirm} className="min-h-11 bg-red-500 px-4 text-xs font-extrabold text-white hover:bg-red-400">Confirm SOS</button></>}><p className="text-sm leading-6 text-white/60">This captures your current GPS coordinates and starts the escalation chain. If the network is offline, the event will be stored locally and synced automatically.</p></Modal></Shell>;
}

export function TouristProfile() {
  const tourist = usePrimaryTourist();
  return <Shell eyebrow="Tourist workspace" title="Profile"><TouristHeader eyebrow="Your account" title="The details that help responders help you." description="Keep emergency information current. Only the minimum required details are surfaced during a verified response." action={<button className="inline-flex min-h-11 items-center gap-2 border border-sy-border px-4 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><UserRound size={15} /> Edit profile</button>} /><div className="grid gap-4 lg:grid-cols-[0.75fr_1.25fr]"><Panel eyebrow="Identity" title="SafeYatra profile"><div className="flex items-center gap-4 border-b border-sy-border pb-5"><div className="grid h-14 w-14 place-items-center border border-sy-accent/30 bg-sy-accent/10 font-mono text-lg font-bold text-sy-accent">{tourist.initials}</div><div><p className="text-lg font-extrabold text-white">{tourist.name}</p><p className="mt-1 font-mono text-xs text-white/45">{tourist.touristId}</p></div></div><div className="mt-2"><DataRow label="Mobile" value={tourist.phone} /><DataRow label="Emergency contact" value={tourist.emergencyContact} /><DataRow label="Verification" value="Verified ✓" valueClass="text-sy-success" /><DataRow label="Preferred language" value="English" /></div></Panel><Panel eyebrow="Emergency information" title="Shared only when needed"><div className="grid gap-3 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-xs font-bold text-white/60">Full name</span><input defaultValue={tourist.name} className="h-11 w-full border border-sy-border bg-sy-panel px-3 text-sm text-white" /></label><label className="block"><span className="mb-2 block text-xs font-bold text-white/60">Phone</span><input defaultValue={tourist.phone} className="h-11 w-full border border-sy-border bg-sy-panel px-3 text-sm text-white" /></label><label className="block sm:col-span-2"><span className="mb-2 block text-xs font-bold text-white/60">Emergency contact</span><input defaultValue={tourist.emergencyContact} className="h-11 w-full border border-sy-border bg-sy-panel px-3 text-sm text-white" /></label></div><button className="mt-4 min-h-11 bg-sy-accent px-4 text-xs font-extrabold text-sy-bg hover:bg-white">Save changes</button></Panel></div></Shell>;
}
