import { useState } from 'react';
import { Activity, ArrowRight, BadgeCheck, BookOpenCheck, Check, CheckCircle2, Clock3, Database, FileCog, Landmark, LockKeyhole, MoreHorizontal, Plus, ScrollText, Settings2, ShieldCheck, SlidersHorizontal, UsersRound, Wrench } from 'lucide-react';
import Shell from '../components/shared/Shell';
import MapSurface from '../components/shared/MapSurface';
import { DataRow, EmptyState, MetricCard, PageIntro, Panel, StatusPill, Timeline } from '../components/shared/Primitives';
import RiskBadge from '../components/shared/RiskBadge';
import StatusBadge from '../components/shared/StatusBadge';
import { useApp } from '../store/AppContext';

const adminDashboard = { activeTourists: '1,250', activeEmergencies: 4, criticalRisk: 12, highRisk: 28, offlineTourists: 18 };

function AdminIntro({ eyebrow, title, description, action }) { return <PageIntro eyebrow={eyebrow} title={title} description={description} action={action} />; }

export function AdminDashboard() {
  const { state } = useApp();
  return <Shell eyebrow="System administration" title="Platform overview" actions={<StatusPill tone="safe" icon={ShieldCheck}>All services healthy</StatusPill>}><AdminIntro eyebrow="Control plane" title="The safety network, at a glance." description="Keep the people, zones, policies and audit trail that power SafeYatra aligned." action={<button className="inline-flex min-h-11 items-center gap-2 border border-sy-border px-4 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><Settings2 size={15} /> System settings</button>} /><div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-5"><MetricCard label="Active tourists" value={adminDashboard.activeTourists} detail="Across 12 zones" tone="accent" icon={UsersRound} /><MetricCard label="Open emergencies" value={adminDashboard.activeEmergencies} detail="3 critical now" tone="critical" icon={Activity} /><MetricCard label="Critical risk" value={adminDashboard.criticalRisk} detail="Escalation threshold" tone="critical" icon={ShieldCheck} /><MetricCard label="Offline queue" value={adminDashboard.offlineTourists} detail="Packets waiting" tone="neutral" icon={Database} /><MetricCard label="Resolution rate" value="94.2%" detail="Last 30 days" tone="safe" icon={CheckCircle2} /></div><div className="mt-4 grid gap-4 xl:grid-cols-[1.35fr_0.65fr]"><MapSurface title="Network coverage" subtitle="All monitored locations and response zones" tourists={state.tourists} zones={state.zones} incidents={state.incidents.filter((incident) => incident.status !== 'resolved')} height="440px" /><div className="space-y-4"><Panel eyebrow="Service health" title="Production readiness"><div className="space-y-3">{[['API gateway', '99.98%', 'safe'], ['AI risk engine', '99.4%', 'safe'], ['Sync worker', '97.8%', 'caution'], ['Identity registry', '100%', 'safe']].map(([label, value, tone]) => <div key={label} className="flex items-center justify-between gap-3 border-b border-sy-border pb-3 last:border-0 last:pb-0"><span className="text-xs text-white/55">{label}</span><span className={`font-mono text-xs font-bold ${tone === 'safe' ? 'text-sy-success' : 'text-sy-warning'}`}>{value}</span></div>)}</div></Panel><Panel eyebrow="Recent audit" title="Last admin actions"><Timeline compact items={[{ status: 'resolved', label: 'Geo-fence updated', time: '08:39', actor: 'System Administrator' }, { status: 'acknowledged', label: 'Responder role granted', time: '08:31', actor: 'System Administrator' }, { status: 'detected', label: 'Offline sync reconciled', time: '08:26', actor: 'Sync worker' }]} /></Panel></div></div></Shell>;
}

export function AdminUsers() {
  const { state } = useApp();
  const [query, setQuery] = useState('');
  const users = [...state.tourists.map((tourist) => ({ name: tourist.name, role: 'tourist', status: tourist.isOnline ? 'Active' : 'Offline', id: tourist.touristId })), { name: 'Nashik Control Room', role: 'authority', status: 'Active', id: 'AUTH-001' }, { name: 'Ravi Patil', role: 'responder', status: 'Active', id: 'RESP-007' }, { name: 'System Administrator', role: 'admin', status: 'Active', id: 'ADM-001' }].filter((user) => `${user.name} ${user.id}`.toLowerCase().includes(query.toLowerCase()));
  return <Shell eyebrow="System administration" title="Users"><AdminIntro eyebrow="Access management" title="Every account has a clear boundary." description="Review role access and session health. The prototype keeps this data local and ready for a real identity provider." action={<div className="flex gap-2"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search users" className="h-11 w-44 border border-sy-border bg-sy-panel px-3 text-xs text-white placeholder:text-white/25" /><button className="grid h-11 w-11 place-items-center bg-sy-accent text-sy-bg hover:bg-white" aria-label="Add user"><Plus size={16} /></button></div>} /><div className="overflow-x-auto border border-sy-border"><table className="w-full min-w-[680px] text-left"><thead className="bg-sy-panel text-[10px] uppercase tracking-[0.12em] text-white/40"><tr><th className="px-4 py-3">User</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">ID</th><th className="px-4 py-3" /></tr></thead><tbody className="divide-y divide-sy-border/60">{users.map((user) => <tr key={user.id} className="bg-sy-card"><td className="px-4 py-4 text-xs font-bold text-white">{user.name}</td><td className="px-4 py-4"><StatusPill tone={user.role === 'authority' ? 'blue' : user.role === 'responder' ? 'caution' : user.role === 'admin' ? 'critical' : 'safe'}>{user.role}</StatusPill></td><td className="px-4 py-4 text-xs text-white/55">{user.status}</td><td className="px-4 py-4 font-mono text-[10px] text-white/40">{user.id}</td><td className="px-4 py-4 text-right"><button className="text-white/40 hover:text-white" aria-label={`More actions for ${user.name}`}><MoreHorizontal size={17} /></button></td></tr>)}</tbody></table></div></Shell>;
}

export function AdminZones() {
  const { state } = useApp();
  const [selected, setSelected] = useState(state.zones[0]);
  return <Shell eyebrow="System administration" title="Zones"><AdminIntro eyebrow="Policy management" title="Keep risk boundaries clear and current." description="Manage the zones that shape local risk calculation, tourist prompts and authority map coverage." action={<button className="inline-flex min-h-11 items-center gap-2 bg-sy-accent px-4 text-xs font-extrabold text-sy-bg hover:bg-white"><Plus size={15} /> Add zone</button>} /><div className="grid gap-4 xl:grid-cols-[1.1fr_0.9fr]"><Panel eyebrow="All configured zones" title={`${state.zones.length} active geo-fences`}><div className="space-y-2">{state.zones.map((zone) => <button key={zone.id} onClick={() => setSelected(zone)} className={`flex w-full items-center justify-between gap-3 border px-3 py-3 text-left ${selected.id === zone.id ? 'border-sy-accent/40 bg-sy-accent/[0.05]' : 'border-sy-border bg-sy-panel hover:border-sy-accent/30'}`}><div className="flex min-w-0 items-center gap-3"><span className={`h-2.5 w-2.5 rounded-full ${zone.type === 'danger' ? 'bg-sy-critical' : zone.type === 'caution' ? 'bg-sy-warning' : zone.type === 'restricted' ? 'bg-sy-offline' : 'bg-sy-success'}`} /><div><p className="text-xs font-bold text-white">{zone.name}</p><p className="mt-1 font-mono text-[10px] text-white/40">{zone.type} · r={zone.radius}km</p></div></div><ArrowRight size={14} className="text-white/35" /></button>)}</div></Panel><Panel eyebrow="Zone inspector" title={selected.name}>{selected && <><DataRow label="Type" value={selected.type} /><DataRow label="Coordinates" value={`${selected.latitude.toFixed(4)}, ${selected.longitude.toFixed(4)}`} mono /><DataRow label="Radius" value={`${selected.radius} km`} mono /><div className="mt-4 border border-sy-border bg-sy-bg-soft p-4"><p className="sy-label">Description</p><p className="mt-2 text-xs leading-5 text-white/55">{selected.description}</p></div><div className="mt-4 flex gap-2"><button className="min-h-10 flex-1 border border-sy-border text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white">Edit zone</button><button className="min-h-10 flex-1 border border-red-300/30 text-xs font-bold text-red-100 hover:bg-red-400/10">Deactivate</button></div></>}</Panel></div></Shell>;
}

export function AdminSettings() {
  const [saved, setSaved] = useState(false);
  return <Shell eyebrow="System administration" title="Settings"><AdminIntro eyebrow="System configuration" title="Make the defaults explicit." description="Configure how the demo describes risk, synchronizes local events and surfaces operator notifications." action={saved ? <StatusPill tone="safe" icon={Check}>Saved locally</StatusPill> : <button onClick={() => setSaved(true)} className="min-h-11 bg-sy-accent px-4 text-xs font-extrabold text-sy-bg hover:bg-white">Save settings</button>} /><div className="grid gap-4 lg:grid-cols-2"><Panel eyebrow="Safety defaults" title="Risk & escalation"><div className="space-y-5"><label className="flex items-center justify-between gap-4"><span><span className="block text-xs font-bold text-white">Auto-escalate critical risk</span><span className="mt-1 block text-[10px] text-white/40">Notify authority when risk reaches 80%.</span></span><input defaultChecked type="checkbox" className="h-5 w-5 accent-sy-accent" /></label><label className="flex items-center justify-between gap-4"><span><span className="block text-xs font-bold text-white">Offline SOS queue</span><span className="mt-1 block text-[10px] text-white/40">Keep SOS available without network.</span></span><input defaultChecked type="checkbox" className="h-5 w-5 accent-sy-accent" /></label><label className="block"><span className="mb-2 block text-xs font-bold text-white">Risk warning threshold</span><input defaultValue="60" type="range" min="0" max="100" className="w-full accent-sy-accent" /><div className="mt-2 flex justify-between font-mono text-[10px] text-white/40"><span>0%</span><span>60%</span><span>100%</span></div></label></div></Panel><Panel eyebrow="Notification policy" title="Operator signals"><div className="space-y-3">{['Critical incidents', 'Offline tourist queue', 'Geo-fence updates', 'Daily analytics digest'].map((item, index) => <div key={item} className="flex items-center justify-between border-b border-sy-border pb-3 last:border-0 last:pb-0"><div><p className="text-xs font-bold text-white">{item}</p><p className="mt-1 text-[10px] text-white/40">{index === 0 ? 'Immediate / all channels' : index === 1 ? 'Every 10 minutes' : 'Control room only'}</p></div><button className="text-xs font-bold text-sy-accent">Configure</button></div>)}</div></Panel><Panel eyebrow="Integrations" title="Backend handoff"><div className="space-y-1"><DataRow label="API base URL" value="/api" mono /><DataRow label="Socket transport" value="WebSocket + fallback" /><DataRow label="Offline store" value="IndexedDB / local queue" /><DataRow label="Identity registry" value="Blockchain adapter ready" /></div></Panel><Panel eyebrow="Data retention" title="Privacy controls"><div className="space-y-3"><div className="border border-sy-accent/25 bg-sy-accent/[0.05] p-4"><div className="flex gap-3"><LockKeyhole size={17} className="mt-0.5 text-sy-accent" /><p className="text-xs leading-5 text-white/55">Sensitive profile fields are not used in the public landing view and are excluded from the verification receipt by default.</p></div></div><button className="min-h-10 border border-sy-border px-3 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white">Review retention policy</button></div></Panel></div></Shell>;
}

export function AdminAuditLogs() {
  const logs = [
    { status: 'resolved', label: 'Incident INC-2026-001 acknowledged', time: '08:41:28', actor: 'Nashik Control Room', detail: 'Authority action' },
    { status: 'acknowledged', label: 'Unit Alpha-07 assigned', time: '08:40:12', actor: 'Ravi Patil', detail: 'Responder action' },
    { status: 'detected', label: 'Offline packet queue reconciled', time: '08:36:02', actor: 'Sync worker', detail: 'System action' },
    { status: 'resolved', label: 'Kalsubai caution zone updated', time: '08:31:49', actor: 'System Administrator', detail: 'Configuration action' },
    { status: 'acknowledged', label: 'Digital ID hash verified', time: '08:28:12', actor: 'Nashik Control Room', detail: 'Identity action' }
  ];
  return <Shell eyebrow="System administration" title="Audit logs"><AdminIntro eyebrow="Accountability" title="Know who changed what, and when." description="A readable event trail for safety operations, identity checks, configuration changes and offline sync." action={<button className="inline-flex min-h-11 items-center gap-2 border border-sy-border px-4 text-xs font-bold text-white/70 hover:border-sy-accent/40 hover:text-white"><DownloadIcon /> Export logs</button>} /><div className="grid gap-4 lg:grid-cols-[1.3fr_0.7fr]"><Panel eyebrow="System event trail" title="Latest activity"><Timeline items={logs} /></Panel><div className="space-y-4"><Panel eyebrow="Log health" title="Coverage"><MetricCard label="Events today" value="2,438" detail="Across all roles" tone="accent" icon={ScrollText} /><div className="mt-2 grid grid-cols-2 gap-2"><MetricCard label="Operator" value="1,692" tone="blue" icon={UsersRound} /><MetricCard label="System" value="746" tone="neutral" icon={Database} /></div></Panel><Panel eyebrow="Audit controls" title="Filter trail"><div className="space-y-2"><button className="flex min-h-10 w-full items-center justify-between border border-sy-accent/30 bg-sy-accent/[0.05] px-3 text-xs font-bold text-sy-accent">All events <Check size={14} /></button><button className="flex min-h-10 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/60 hover:text-white">Operator actions <ArrowRight size={14} /></button><button className="flex min-h-10 w-full items-center justify-between border border-sy-border px-3 text-xs font-bold text-white/60 hover:text-white">System actions <ArrowRight size={14} /></button></div></Panel></div></div></Shell>;
}

function DownloadIcon() { return <span className="font-mono text-[10px]">CSV</span>; }
