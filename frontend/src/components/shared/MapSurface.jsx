import { CircleDot, MapPinned, Navigation, ShieldCheck } from 'lucide-react';
import LiveMap from './LiveMap';
import MapLegend from './MapLegend';

export default function MapSurface({ tourists = [], zones = [], incidents = [], center, routePoints = [], height = '480px', title = 'Live tactical map', subtitle = 'Nashik safety region / GPS stream active', overlay = null }) {
  return <div className="border border-sy-border bg-sy-panel p-3"><div className="mb-3 flex flex-col justify-between gap-3 px-1 sm:flex-row sm:items-center"><div><p className="sy-label text-sy-accent">{title}</p><p className="mt-1 text-xs text-white/45">{subtitle}</p></div><div className="flex items-center gap-3"><span className="flex items-center gap-1.5 text-[10px] text-emerald-200"><span className="h-2 w-2 rounded-full bg-sy-success" />GPS active</span><span className="font-mono text-[10px] text-white/35">20.0059, 73.7897</span></div></div><div className="relative overflow-hidden border border-sy-border"><LiveMap tourists={tourists} zones={zones} incidents={incidents} center={center} routePoints={routePoints} height={height} />{overlay && <div className="absolute left-3 top-3 z-10 max-w-[260px] border border-sy-border bg-sy-bg/90 p-3 shadow-xl backdrop-blur">{overlay}</div>}</div><div className="mt-3 flex flex-wrap items-center justify-between gap-3"><MapLegend /><div className="flex items-center gap-3 text-[10px] text-white/40"><span className="flex items-center gap-1"><Navigation size={12} /> Route trail</span><span className="flex items-center gap-1"><ShieldCheck size={12} /> Verified devices</span><span className="flex items-center gap-1"><CircleDot size={12} /> Incident marker</span></div></div></div>;
}
