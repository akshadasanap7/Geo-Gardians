import { Link, NavLink, useLocation } from 'react-router-dom';
import { ArrowRight, Shield, Wifi } from 'lucide-react';
import { publicNavigation } from '../../config/navigation';

export default function PublicLayout({ children }) {
  const location = useLocation();
  return <div className="min-h-screen overflow-hidden bg-sy-bg text-sy-text"><header className="sticky top-0 z-40 border-b border-sy-border bg-sy-bg/90 backdrop-blur"><div className="mx-auto flex min-h-[72px] max-w-7xl items-center justify-between gap-6 px-4 sm:px-6 lg:px-8"><Link to="/" className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center border border-sy-accent/40 bg-sy-accent/10 text-sy-accent"><Shield size={19} /></span><span><span className="block text-sm font-extrabold text-white">SafeYatra <span className="text-sy-accent">AI</span></span><span className="block text-[9px] uppercase tracking-[0.18em] text-white/40">Safety network</span></span></Link><nav className="hidden items-center gap-6 md:flex" aria-label="Public navigation">{publicNavigation.map((item) => <NavLink key={item.href} to={item.href} className={() => `text-sm font-semibold transition ${location.pathname === item.href || (item.href.includes('#') && location.pathname === '/') ? 'text-white' : 'text-white/50 hover:text-white'}`}>{item.label}</NavLink>)}</nav><div className="flex items-center gap-2"><span className="hidden items-center gap-1.5 text-[10px] font-bold uppercase tracking-[0.1em] text-emerald-300 sm:flex"><Wifi size={13} /> System operational</span><Link to="/login" className="inline-flex min-h-10 items-center gap-2 border border-sy-accent/40 bg-sy-accent px-3 text-xs font-bold text-sy-bg transition hover:bg-white">Open demo <ArrowRight size={14} /></Link></div></div></header>{children}<footer className="border-t border-sy-border bg-sy-bg-soft"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 px-4 py-8 sm:flex-row sm:items-center sm:px-6 lg:px-8"><div><p className="text-sm font-extrabold text-white">SafeYatra <span className="text-sy-accent">AI</span></p><p className="mt-1 text-xs text-white/40">Built for resilient travel across India.</p></div><p className="font-mono text-[10px] uppercase tracking-[0.1em] text-white/35">DETECT → WARN → RESPOND → SYNC</p></div></footer></div>;
}
